@model IEnumerable<Ogrenci>
@{
    ViewData["Title"] = "OgrenciListe";
}

<h1>Ogrenci Listesi</h1>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Ad</th>
            <th>Soyad</th>
            <th>Numara</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Ad</td>
                <td>@item.Soyad</td>
                <td>@item.Numara</td>
                <td><a class="btn btn-sm btn-primary text-white">Düzenle</a>  <a class="btn btn-sm btn-danger text-white">Sil</a></td>
            </tr>
        }
    </tbody>
</table>